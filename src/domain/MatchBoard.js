import PairMatching from "./PairMatching.js";

class MatchBoard {
  frontendMap = new Map();
  backendMap = new Map();

  constructor() {

  }

  record(course, level, mission) {
    if (course === 'frontend') {

    }
    
    if (course === 'backend') {
      
    }
  }

  frontendRecord(level, mission) {
    this.frontendMap.set([level, mission], )

  }

  backendRecord() {

  }

  


}

export default MatchBoard;
